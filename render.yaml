services
type: web
name: abkdarrk-terminal-api
env: python" >> render.yaml
plan: free" >> render.yaml
buildCommand: pip install -r requirements.txt
startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
